
1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll? 

*.getElementById?
ans:Finds an element with a specific ID.

*.getElementsByClassName?
ans:Retrieves all elements with the same class name.

*.querySelector?
ans:id, class, tag all selectors can be used

*.querySelectorAll?
ans: Extracts all matching elements with a CSS selector. Returns multiple elements.


2. How do you create and insert a new element into the DOM?
  ans: Creates new element. Adds content. Adds to DOM.



3. What is Event Bubbling? And how does it work?
 *ans: Event bubbling is a process where when an event occurs on a child element, that event continues to propagate up to its parent, grandparent, body, and document.

 4. What is Event Delegation in JavaScript? Why is it useful?
 ans: Event Delegation is a method where we set an event listener on the parent element at once, instead of providing separate events for child elements.

 5. What is the difference between preventDefault() and stopPropagation() methods?
    ans: Disables the default browser behavior of an element.